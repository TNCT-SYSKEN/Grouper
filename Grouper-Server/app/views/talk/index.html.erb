<div class="container talk_page">
  <div class="row talk">
    <% @talks.each do |talk| %>
      <div class="user">
        <p><%= user_email(talk.user_id) %></p>
      </div>
      <div class="content">
        <p><%= talk.talk %></p>
      </div>
      <br>
    <% end %>
    <div id="uid" style="display:none;"><%= current_user.id %></div>
    <div id="gid" style="display:none;"><%= @group.id %></div>

    <%= form_tag('new', remote: true, class: "foot form-horizontal") do %>
      <div class="form-group form-group-sm">
        <div class="btn btn-success col-sm-2">追加</div>
        <div class="col-sm-8">
          <%= text_field_tag :talk, "", id: "text", class: "form-control" %>
        </div>
        <%= submit_tag "送信", id: "send", class: "btn btn-primary col-sm-2" %>
      </div>
    <% end %>
  </div>
  <script src="/talk.js"></script>
</div>
